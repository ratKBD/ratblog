import classes from "./headers.module.css";

const Header = () => {
  return (
    <div className={classes.head}>
      <h1 className={classes.colour}>My Blog</h1>
    </div>
  );
};

export default Header;
